# Public browser setting. Never put secrets in VITE_ variables.
# Keep the API on the same origin as the frontend.
VITE_API_BASE_URL=/api/v1

# Development proxy defaults to http://127.0.0.1:8000.
# To override, set BACKEND_PROXY_TARGET in the terminal before npm run dev.
